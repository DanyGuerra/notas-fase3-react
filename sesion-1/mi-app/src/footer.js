const Footer = (props) => (
  <footer style={{
      backgroundColor: "#DDD",
      display: "flex",
      justifycontent: "center",
      padding: 10,
    }
  }>
    trademark Bedu {props.year}

  </footer>
)

export default Footer